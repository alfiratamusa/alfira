funcation formatNama(user) {
    return user.fristName + ' ' + user,lastName;
}

const user = {
    fristName: 'Alfira',
    lastName: 'Tamusa'
}

const element = (
    <h1>
        Hello, {formatName(user)}!
    </h1>
);


function Tiga() (
    return (
        element
    );
)


export default Tiga;